#include <stdio.h>
int main ()
{
	int x,y,a,b,c,m,n;
	if (n>3 && n<1000)
		scanf ("%d",&x);
    for (n=0; n<x; x++)
    {
        scanf ("%d %d %d",&a,&b,&c);
        n = (a*4)+(b*2)+c;
        b[m] = n;
    }
    printf ("%d %d",);
    return 0;
}
